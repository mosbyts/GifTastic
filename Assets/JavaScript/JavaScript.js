var topics = [Pumpkins, Bats, Clowns, Witches, candyCorn];
var button = "<button>";
//a loop that appends a button for each string in the array.

for (var i = 0; i > topics.length; i++);
    i.append(button);

button.onclick();